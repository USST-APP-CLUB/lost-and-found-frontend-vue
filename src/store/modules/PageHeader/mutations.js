// export const updateStatusBarHeight = (state, statusBarHeight) => {
//     state.statusBarHeight = statusBarHeight
// }

export const updateHeadNavHeight = (state, headNavHeight) => {
    state.headNavHeight = headNavHeight;
}

export const updateHeadContentHeight = (state, headContentHeight) => {
    state.headContentHeight = headContentHeight;
}

export const updateHeadContentOffset = (state, headContentOffset) => {
    state.headContentOffset = headContentOffset;
}